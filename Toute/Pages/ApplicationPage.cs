namespace Toute
{
    /// <summary>
    /// Application page enum that
    /// will be used every time if page
    /// should be changed
    /// </summary>
    public enum ApplicationPage
    {
        //Login Page
        LoginPage = 1,

        //Register Page
        RegisterPage = 2
    }
}
